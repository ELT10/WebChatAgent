# Core dependencies
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
websockets>=12.0

# LangChain and AI
langchain>=0.1.0
langchain-community>=0.0.13
langchain-openai>=0.0.2
langchain-core>=0.1.10
openai>=1.6.1

# Vector store and embeddings
chromadb>=0.4.22

# Web scraping
playwright>=1.40.0
beautifulsoup4>=4.12.2
trafilatura>=1.8.0  # Updated for Python 3.12 compatibility
pytesseract>=0.3.10
Pillow>=10.1.0
lxml>=5.0.0  # Explicitly require Python 3.12 compatible version

# Translation
deep-translator>=1.11.4
indic-transliteration>=2.3.59

# Utilities
python-dotenv>=1.0.0
pydantic>=2.5.3
requests>=2.31.0
aiohttp>=3.9.1
psutil>=5.9.6

# Optional: For crawl_agent.py (not currently integrated)
# crawl4ai>=0.3.0  # Uncomment if you plan to use crawl_agent.py

# Cost Optimization (optional but recommended)
sentence-transformers>=2.2.2  # For FREE local embeddings (saves 100% embedding costs)
huggingface-hub>=0.23.0
torch>=2.1.0; platform_system!="Darwin" or platform_machine!="arm64"  # CPU ok; mac arm64 handled by pip
torch>=2.2.0; platform_system=="Darwin" and platform_machine=="arm64"  # Apple Silicon compatible versions

